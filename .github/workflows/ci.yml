name: CI
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: actions/setup-node@v4
    - name: Install dependencies
      run: make install
    - name: Run tests
      run: make test
